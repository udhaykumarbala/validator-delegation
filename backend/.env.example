# Server Configuration
PORT=3000

# Simple Admin Password for Frontend Auth (Change this in production!)
ADMIN_PASSWORD=admin123

# API Access Password for public endpoints (Change this in production!)
API_ACCESS_PASSWORD=your-secure-api-key-here

# Database Path (SQLite)
DB_PATH=./validator_requests.db